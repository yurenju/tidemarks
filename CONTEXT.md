# Tidemarks — Domain Glossary

專案共用術語表。skill 產出（issue 標題、重構提案、測試名稱）都用這裡定義的詞。

**每個詞條有中英文兩半，斜線分開。** 中文那半給中文文件用，英文那半給英文的註解、識別字與測試
名稱用。兩半指的是同一個概念，不是兩個詞。

**程式碼裡有名字的，英文那半就是那個名字**（〈手勢 / Gesture〉對著 `gesture.ts`、〈重點 / Mark〉
對著 `markVar()`），所以識別字改名的時候這裡要跟著改。程式碼裡還沒有東西的概念（〈退路 / Exit〉、
〈寄物櫃 / Locker〉這些決策層的詞）英文名是先取的，**哪天真的長出程式碼就用它**，不要另外再想一個。
兩邊已經對不上的地方會在條目裡寫明，現在有兩處：〈自帶字型 / Carried font〉與〈回訪模式 / Visit mode〉。

有幾個詞條**只有英文**（`Tidemarks`、`chrome`、`Scrubber`、`Navigator`、`Highlight layer`、
`catalog`），那是刻意的：`Tidemarks` 是名字，其餘幾個能想到的中文譯名每一個都比原詞模糊，理由寫在
各自的條目裡。

渲染層是 [frond](packages/frond/README.md)（見 [ADR-0002](docs/adr/0002-epub-ts-to-frond.md) 與 [ADR-0017](docs/adr/0017-frond-moves-in-and-stops-being-published.md)），
它自己有一份 CONTEXT.md 定義「頁」、「fraction」、「CFI」、「書寫模式」這些**書與版面**的詞。
這份表只定義 Tidemarks 這一層的詞：**決策**與**介面**。兩邊詞不重複，是刻意的：一個詞只有一個擁有者。
唯一的例外是〈自帶字型 / Carried font〉，兩邊各講同一件事的一端，理由寫在 frond 那份的條目裡。

## 名字 / The name

### Tidemarks

這個 app 的名字，2026-08-19 定案（[ADR-0019](docs/adr/0019-the-app-is-called-tidemarks.md)）。
敘述裡寫 `Tidemarks`，識別字、套件名與網域寫 `tidemarks`。潮痕，指潮水退去後留在岸上的那條線：
**來來去去，而痕跡慢慢變深**。

**檔案裡已經改完了。** `git grep -i folis` 還找得到的只有刻意留著的兩種：**兩套** `docs/adr/`
（根目錄與 frond 各一）的內文與檔名，以及像這一節這樣**在講改名這件事本身**的句子。指向舊 repo 的
連結已經全部清掉了。

**hostname 是 `app.tidemarks.io`**，官方那一台，兩份 README 指的就是它，2026-08-21 起真的回應請求。
hostname 是這次改名唯一不可逆的一步：**WebAuthn 的 RP ID 有 credential 之後就不能改**，所以舊網域
上註冊過的 passkey 一把都沒帶過來，那是換 hostname 本來就要付的代價，不是哪裡出了問題。

**一個名字，不是兩個**，它同時是軟體與服務，「官方那一台」與「自己架的那一份」的區別由主機名
扛。apex 留給官方網站，app 住在子網域底下。

**2026-08-11 以前它叫 spine，之後到 2026-08-19 叫 Folis**，兩次改名的理由與這次取名要跑的兩條
檢查都收在 [ADR-0019](docs/adr/0019-the-app-is-called-tidemarks.md)。那些日期以前寫下的 `spine`
與 `Folis` 就是 Tidemarks。

_Avoid_: spine（**現在只指 EPUB 封裝格式裡的 `<spine>` 元素**，而 frond 連那個意思都改用
`readingOrder`）、Spine、Folis（都是舊名字，寫下來的時候一定是在講改名這件事本身）、
Tidemark 單數（複數是刻意的，指的是累積的那些痕跡，不是那個現象）

## 讀者的動作 / What the reader does

### 手勢 / Gesture

讀者在書上做的每一個動作**代表什麼意思**，以及那些意思互相牴觸的時候誰贏。住在
`packages/app/src/lib/gesture.ts`，`createGestureMachine(nav, opts)` 造出來，`.send(event)` 吃事件、
吐 intent。

跟 `touch.ts` 的分工一句話講得完：**`touch.ts` 答「這一下算什麼」，手勢機答「一連串下來代表什麼」。**
前者是算術（10px 算拖曳、半秒算長按、1/3 頁算翻），後者是把那些算術串成一段時間裡的狀態：按下去
猶豫三秒再滑、拖到一半反向、長按選字選到一半系統把手指收走。

- **它不碰 timer。** 長按的 `setTimeout` 在 Reader 裡，到期時送一個 `longPressFired` 進來。這樣
  「什麼情況下這個 timer 該被取消」變成一條一條有名字的轉移，而不是散在檔案裡的六次
  `clearTimeout`，**漏掉一次沒有人回報得出來**，而按住多久算長按錯了讀者第一下就有感。這是刻意的
  取捨，代價是那個數字本身沒有單元測試。
- **它不碰 frond 的物件。** `TurnInProgress` 由〈翻頁編排〉保管，手勢機只說 `beginTurn` /
  `moveTurn` / `commitTurn` / `cancelTurn` / `dropTurn`，再被告知那個 turn 量到什麼。要讓手勢機
  自己拿著那個物件，就得為了測試假造一個，而它最難假裝對的部分（`live` getter、commit 之後的
  行為）正是最關鍵的部分。
- **它不量東西。** 頁寬、是不是抵到書首書尾、手指底下有沒有重點，全部隨事件帶進來。頁寬會變
  （轉螢幕、桌機開面板時書讓出一欄），建構時記住的值會過期而且過期時沒有東西會紅。
- **`preventDefault` 跟 intent 分開回傳。** intent 是「要對書做的事」，`preventDefault` 是「對這個
  事件的回答」，而且必須同步交還給瀏覽器。混進同一個陣列，就會有一個成員的語意跟其他成員不同。
- **intent 是扁平的 union**，一個動作一個 `kind`。Reader 那邊的 `switch` 漏掉一種會在編譯期紅，
  不是在手機上紅。
- **桌機也走這裡**：方向鍵與頁鈕是 `side` 事件，滑鼠的分支很短（不跟手、不長按、只 tap 叫 chrome），
  但「這一下算不算觸控」本身就是一條規則，留在外面等於留在沒有測試的那一側。

底下是它裁決的那些規則本身：

- **翻頁只有拖曳**（[ADR-0024](docs/adr/0024-turning-a-page-is-a-swipe-and-nothing-else.md)）：
  手指按在書上滑，書頁跟著手指走、看得到鄰頁，放手時決定翻或不翻。tap 一律叫出 chrome，不分區。
  桌機走左右箭頭與頁鈕；滑鼠既不 tap 翻頁也不拖曳翻頁。規則全文在
  [docs/specs/swipe-to-turn/spec.md](docs/specs/swipe-to-turn/spec.md)。
- **什麼時候開始跟手**：位移超過 `TAP_SLOP_PX`（10px）而且是橫向的。**時間不參與判斷**：按下去
  猶豫三秒再滑一樣跟手。用一條讀者感覺不到的時間線把人擋在外面，症狀會是「這 app 有時候翻不了
  頁」，而那種 bug 沒有人回報得出來。
- **選取決定這一次按壓屬於誰**：按下那一刻已經有選取，讀者就是在調整它，頁面不動；沒有就跟手。
  ⚠️ **這條只在瀏覽器自己畫選取的時候成立**（桌機）。選取是我們自己畫的時候，兩端的把手會自己接走
  按在它們身上的按壓，所以按在別的地方就是翻頁，選取跟著那一頁走。
- **放手怎麼判**：走過頁距的 1/3，或放手瞬間速度夠快（輕彈），任一條成立就翻。兩個數字都是猜的。
- **按壓不讓瀏覽器當成自己的 tap**：Chrome for Android 會在單純一下 tap 上
  選走一個詞並升起搜尋 bar，而那個 bar 收拾不掉（它不是文件的一部分）。條件是**手指或觸控筆、不是
  連結**，跟位置無關。tap 不再翻頁之後這條**一個字都沒改**，換的只是它保護的東西：從「這一下要翻的
  頁」變成「這一下要叫的 chrome」。
  **機制是量出來的，不是照文件寫的**：
  Chrome 文件說「文字不可選取就不會觸發」，實機上那條只把冒出 bar 的比例從 72% 壓到 21%；真的
  擋住的是取消那一下 tap 的預設動作，0/15。代價是那一下沒有 `click`，所以「不是連結」這個條件
  從「反正也沒理由擋」變成**非有不可**：沒有它，書裡的註腳連結會點不開。
- **手勢從三個面進來**：書的文件（frond 的 pointer 事件）、書框外的留白帶（Reader 自己掛在容器上的
  listener）、以及選取兩端的把手（Reader 自己畫的元素，會 capture pointer）。前兩者會合在同一組
  處理，因為 iframe 的邊界不讓事件出來、容器也收不到裡面的，而手機上那條留白帶有 32px 寬，正好是
  拇指構得到的地方。兩個面都要 `touch-action: none`，否則瀏覽器會在 script 聽到之前把手指判給自己
  的捲動（真機量到的序列與守門的測試見 spec）。還有第四種：**手指在三個面都構不到的地方放開**
  （平板上書兩側的頁鈕），那一下只用來讓進行中的選取落定。

_Avoid_: 按壓（裝不下方向鍵與頁鈕，而它們也走這台狀態機）、`Pager`／`RenditionPort`（都不存在了）

### 翻頁編排 / Turn choreography

〈手勢〉判斷完「這一下是要翻頁」之後，**真的讓那一頁動起來**的那一半。住在
`packages/app/src/lib/turn.ts`，`createTurnRunner(deps)` 造出來，`.run(intent)` 吃〈手勢〉吐出來的
六個翻頁 intent。

跟〈手勢〉的分工一句話講得完：**〈手勢〉答「那代表什麼」，〈翻頁編排〉答「那就做出來」。**
兩個都不決定哪一邊是下一頁，那是 Navigator 的。

- **`TurnInProgress` 由它保管。** frond 交出來的那個半翻頁物件從頭到尾在這裡，〈手勢〉只被告知
  它量到什麼（頁距、有沒有抵到書的一端）。
- **手指以外的翻頁也走這裡。** 方向鍵與頁鈕要的是同一種翻頁，只是由時鐘播、不是由手指播。所以
  「翻一頁就把 chrome 收起來」這種事在兩條路上是同一段程式碼。
- **它守著幾條只有時間看得出來的規則**：書的盡頭那一下要盪出去再回來，不能完全不動，因為按了
  沒反應跟壞掉分不出來；連按方向鍵時，下一次翻頁開始前要先讓上一次落地，否則書會在按住的鍵
  底下停著；讀者要求減少動態時直接落定。
- **鄰頁還沒排好就不滑**，直接換過去。滑一張還是空白的紙不是動畫，是閃一下。
- **它證得了的只有「有沒有照著時間軸下指令」**，證不了 frond 收到之後做對沒有。後者是容器裡那套
  瀏覽器測試守的，兩邊各守一半。

_Avoid_: 翻頁動畫（動畫只是它的一部分；末頁回彈與「不滑直接換」都不是在播動畫）

### Navigator

擁有「這本書哪一邊是下一頁」，就這一件事。三個成員（`onSide`／`dragTowards`／`edgeFor`）共用一份
`rtl`，因為一本書把其中一個反轉了、另一個沒有，下一頁就會從它正要離開的那一邊滑進來。

**「這一下算什麼手勢」不在它身上**，那是〈手勢〉。它只回答方向，連翻頁本身都不做，`onSide` 傳回
`"next"` 或 `"prev"`，怎麼翻、要不要順手收 chrome 是問的人的事。

- 純 factory `createNavigator(opts)`，不依賴 React，也不依賴 frond，三個成員都只吃數字與方向，
  傳回方向。**沒有 `Pager` 了**：它以前存在的理由是 `onSide` 要真的去翻，而現在它只回答。
- 方向反轉只在它內部決定一次，它是「這本書怎麼翻」的單一真相。**`dragTowards` 是那條線最容易搞混
  的地方**：往哪一邊算下一頁由書的方向決定，
  鄰頁從哪一邊進來則不是（頁跟著手指走，所以它從手指離開的那一側進來），兩個一起反轉就會讓下一頁
  從錯的邊滑進來。
- **頁鈕與方向鍵也是滑過去的**，不是瞬間切換
  （[docs/specs/desktop-page-turn/spec.md](docs/specs/desktop-page-turn/spec.md)）。同一本書的兩頁
  長得很像，瞬間切換之後讀者手上只有「畫面變了」，那既可以是往前也可以是往後。**跳頁不給動畫**
  （目錄、筆記、定位軸）：滑一頁的距離不代表跨過去的距離，那個動畫是假話。`edgeFor` 是這條路徑
  上跟 `dragTowards` 對應的那一半，而它**要看書的方向**，沒有手指可以問，就只剩書可以問。
- **section 邊界穿越已經不在它身上**：frond 的 `next()`／`previous()` 自己走到隔壁 section。

### 翻頁方向 / Turn direction

哪一邊是「下一頁」。**是整本書的性質，不是某一節的**，來自 package document 的
`page-progression-direction`（`rtl` 就是右開本，left = next）。書沒宣告時（EPUB 2 根本沒這個屬性）
才退回書寫模式：**第一個排成直排的 section** 定案，之後誰都不能再改。

跟**書寫模式**（直排／橫排）分開講，是因為它們真的是兩件事，混在一起會出事：一張整頁圖的分輯扉頁
不掛書的樣式表，就排成橫排，可是它仍然屬於那本右開的書。每載入一節就重算方向，會讓點擊分區在那一頁
左右對調，讀者照平常的位置往前點卻退回去，然後卡在扉頁前後兩頁之間出不來。

fallback 只認直排、不認橫排，也是同一件事的延伸：第一個載入的 section 不是書的第一節，是**讀者上次
停的那一節**（`start: { cfi }`），完全可能就是那種扉頁。

_Avoid_: 直排＝右開（大多數直排書是右開，但那是兩個獨立的事實）

## 材質 / Materials

### 紙／墨／潮 / Paper, Ink, Tide

Tidemarks 視覺上的三個**材質**，是所有視覺判斷的根據（[ADR-0022](docs/adr/0022-the-interface-is-a-print-shop.md)，
規則的展開在 [docs/design-system.md](docs/design-system.md)）。

- **紙 / Paper**＝容器，讀者改不動的：底、卡、封面、面板、bar。三階明度，**階數就是「浮起來」的說法**。
- **墨 / Ink**＝**讀者留下的痕跡**：重點與筆記。
- **潮 / Tide**＝可以動的東西：主要動作、拖曳的手、你在這裡。**一屏上潮色的地方應該數得出來。**

⚠️ **「墨」比字面窄。** 它不是「所有的字」，正文另有四階濃度（`--text-primary` 到
`--text-faint`），那是排版不是材質。這個區別要守住，因為整套色票只有一條硬條件，而它就靠這兩個
字站著：**「我劃的」跟「我可以按的」不可同色。**

三條硬規則：圓角有四階，每一階綁一種東西（紙 6px、控件 3px、軌道 999px、圓點 50%）；
**沒有任何投影**，浮起來靠比較亮的表面說；質感只出現在讀者碰過的地方，介面本身不做貼圖與擬物。

潮這個字是從產品名字來的，而且它跟墨的關係就是要守的那條線：**潮是會動的，墨是動完留下來的。**

_Avoid_: 主題色／theme color（那是一個可以換的變數，而潮是一個**有名額的**語意）、
品牌色（Tidemarks 的品牌是那三個材質加襯線，不是一個色票）、卡片（「卡」在這裡是紙的一種，
不是 Material 那一套的元件）、藍色（畫面上有三個藍，講「藍色」等於沒有指定哪一個）

## 介面的結構 / How the interface is built

### chrome

閱讀時**不常駐**的那一整組介面：頂列（回書架、書名、目錄、筆記、排版、詳情）與底列（章節、
Scrubber）。它不是一塊面板，是一個**狀態**。閱讀器同一時間只成立三個狀態的其中一個，每一層的出現
都是讀者剛剛做的動作換來的：

| 狀態 | 畫面上有什麼 | 怎麼進、怎麼出 |
| --- | --- | --- |
| **讀** | 只有內文 | 預設。翻一頁就回到這裡 |
| **找** | chrome 全出來 | 輕觸畫面上任何一處（桌機是滑鼠點任何一處）；翻頁或再輕觸一次就收 |
| **標** | 顏色列與「加筆記」，貼著選取，放不下時落到畫面靠下的固定線（ADR-0036 的〈顏色列放在哪裡〉） | 有選取就在，取消選取就走。**手指還按著的時候先不出現**（見下） |

〈讀〉與〈找〉之間怎麼換，住在 `packages/app/src/lib/chrome.ts`：一個純函式
`nextChrome(state, event)` 吃現在的狀態跟一個具名事件（`tapped`、`turned`、`jumped`、
`togglePanel`、`panelDismissed`、`selectionArrived`、`openNote`、`editNote`、`noteSaved`），
吐下一個狀態。Reader 只負責說「剛剛發生的是哪一件事」。

- **它不管〈標〉。** 選取本身（矩形、CFI、手指還按著沒有）留在 `Reader.tsx`，理由跟手勢機不碰
  frond 物件同一個：要在 node 假造那包資料，最難假裝對的部分正是最關鍵的部分。這裡有的是那條
  **規則**：選取送達就把 chrome 收掉，事件叫 `selectionArrived`。
- **它是純函式，不是 `createChromeMachine()`。** 沒有 timer、沒有取樣、什麼都不用注入，狀態交給
  React 保管就好；做成物件等於造出兩份狀態，然後要有人負責讓它們不走散。
- ⚠️ **沒有變化的時候回傳原本那個物件。** 讀者大多數時間 chrome 是收著的，而翻一頁就送一次
  `turned`：每次都給新物件的話，每翻一頁多一次整個 Reader 重繪，那是 `turn-pacing.spec.ts`
  量得到、但沒有人會回報的一種變差。
- **「面板收起來之後還記得剛剛是哪一個」也在狀態裡**（`panelKind`）。Base UI 在面板滑出去的
  180ms 期間還讓它掛著，直接讀 `chrome` 的話面板會在退場時變成空盒子。

**頂列答「這是什麼書、我能做什麼」，底列答「我在哪」。** 章節名跟 Scrubber 是同一個問題的兩種
說法，所以它們同一條；書名留在頂列，因為它是開著這本書的期間唯一不會變的東西。兩者擠同一行的
時候（手機 402pt）會各被截一半，那正是分開的理由。底列裡**軸在上、章節名在下且置中**：軸是主角，
章節名是它現在指到哪的註解。

**chrome 浮在書頁上面一階**（`--surface-raised`，ADR-0028）。它是蓋在書上的，不是佔自己的版面，
所以讀者要看得出哪一層在上面。浮的方式只有面，**沒有陰影**，黏在螢幕上的東西不投影（ADR-0022）。

**chrome 從它自己那一邊滑進來，也從那一邊滑回去。** 頂列走上緣，底列走下緣，entries 跟著
它現在站的那一邊（桌機併進頂列同一行、手機在底列上面，ADR-0023）。同一邊的兩條走一樣的距離，
所以它們是一整塊在動，中間不會裂開一條縫露出書。時間跟翻頁一樣是 180ms，因為這也不是要讀者盯著
看的東西；`prefers-reduced-motion` 就直接出現與消失。**收起來的時候 chrome 還在 DOM 裡**，停在
畫面外、`visibility: hidden`，拆掉就沒有東西可以滑出去了。

**〈標〉在手指離開之後才成立。** 〈接管選取〉那一套讓選取在按住的過程中就長出來，而顏色列若在那時
浮出來，會出現在手指底下、還跟著延伸一路跳位置。所以按住的期間畫面上只有反白與兩個端點手把，**放手
的那一刻才進〈標〉**。這不是三狀態的例外：拖曳的那段時間 chrome 已經收掉了，只是〈標〉還沒站起來。

**三個狀態互斥，沒有例外。** chrome 開著的時候讀者選了字，chrome 就收掉換成顏色列。留一個例外
等於留五個，而顏色列的位置本來就是算出來的（`toolbar-position`），多一層要閃避的東西只會讓那段
算不準。

**寫 `chrome`，不翻成中文。**「介面」「外框」「工具層」都指得到它，那正是要避開的事。這份文件裡
Chrome for Android 也會出現，所以**瀏覽器一律寫全名**，單獨的 `chrome` 只有介面這一個意思。

底下這些是 chrome 的內部零件，**不各自成一個詞條**：要理解其中任何一個都得先讀這一節，拆出去會讓
「chrome 是什麼」散在七個地方。但它們在程式碼裡被引用得最多，所以英文名列在這裡：

| 中文 | 英文 | 它在程式碼裡是誰 |
| --- | --- | --- |
| 〈讀〉 | `Read` | `Chrome` 的 `"down"` |
| 〈找〉 | `Find` | `Chrome` 的 `"up"` |
| 〈標〉 | `Marking` | 不在 `Chrome` 裡，選取本身留在 `Reader.tsx` |
| 〈目錄〉 | `Contents` | `PanelKind` 的 `"toc"`，按鈕的可及名稱就是 `Contents` |
| 〈筆記〉面板 | `Notes` | `PanelKind` 的 `"notes"` |
| 〈排版〉 | `Layout` | `PanelKind` 的 `"layout"` |
| （只有英文） | `Type` | 寫筆記的那個輸入框站著的時候 |
| 〈書的詳情〉 | `About` | `AboutBook.tsx`，testid `panel-about` 與 `about-*` |

⚠️ **`Marking` 跟〈重點〉的 `Mark` 只差一個 ing，而它們是兩件事**：`Mark` 是**讀者留下的那個
東西**（`--mark-indigo`、`markVar()`），`Marking` 是**劃的時候畫面長什麼樣**。英文名這樣取是因為
程式碼已經拿 `mark` 指讀者留下的那個東西了，而詞彙表的英文名跟著程式碼走。

⚠️ **`Notes` 是面板，`Note` 是讀者寫下的那一則**（見〈筆記 / Note〉），也只差一個 s。面板叫 `Notes`
是因為 `PANEL_KINDS` 裡已經是 `"notes"`。

_Avoid_: 工具列（那是〈標〉狀態那一條，不是整組）、HUD、overlay（太籠統，目錄與筆記本來就是浮層）

### 兩種面 / Surfaces

畫面與面板，英文是 `Screen` 與 `Panel`。

介面上會蓋出來的東西只有這兩種，**分法是「它跟底下那個畫面有沒有關係」**：

| 種類 | 判斷方法 | 進 hash 的方式 | 成員 |
| --- | --- | --- | --- |
| **畫面** | 跟底下是誰**無關** | 路徑 | 書架、閱讀器、設定 |
| **面板** | 疊在當前畫面上，講的是底下那個畫面裡的某個東西 | query 段 `d=` | 目錄、筆記、排版、書的詳情 |

舊的分法是「偶爾去一次的地方」，它分不出〈設定〉與〈書的詳情〉，所以〈設定〉當年被歸成面板，
一路長到六項排版設定都住在裡面。換成上面這條就分得開了：設定跟你剛才在書架還是在書裡無關，
所以它是一層樓；書的詳情講的就是底下那本書。這條分法也能預測下一個東西該去哪，那是舊那條做
不到的。

⚠️ **這裡本來有第三類，叫〈抽屜〉，那個詞已經刪掉了。** 它當初是用來解釋「為什麼〈書的詳情〉
進 hash 而三個面板不進」，而現在四張面都進 hash、都用同一個元件，那條界線已經沒有東西可分
（[ADR-0046](docs/adr/0046-one-shell-for-every-panel-and-every-panel-in-the-address.md)）。
讀到舊文件裡的「抽屜」，指的就是現在的〈書的詳情〉。

### 面板 / Panel

**疊在當前畫面上的那一張面**，四個成員：〈目錄〉〈筆記〉〈排版〉〈書的詳情〉。
**同一個元件**（`components/Panel.tsx`），不是四個東西：幾套版面就是幾套 bug，而它們回答的是
同一個問題。

**四張面的差別只有一顆 prop，`needs`**，答的是「這張面開著的時候，底下那個東西還有沒有事情要
做」：

| 面 | `needs` | 為什麼 |
| --- | --- | --- |
| 〈排版〉 | `"page"` | 讀者正在調的就是底下那一頁，蓋住它等於蓋住答案（[ADR-0005](docs/adr/0005-the-reader-adjusts-their-own-reading-not-this-book.md)） |
| 〈目錄〉〈筆記〉 | `"book"` | 寬畫面上按下章節之後面板要留著，因為它送你去的地方就在旁邊；窄畫面上沒有欄可以讓 |
| 〈書的詳情〉 | `"nothing"` | 看三個數字然後離開，而且它講的是**某一本書**，底下那個畫面不一定是那本書（在書架上開的時候就不是） |

⚠️ **`needs` 說的是「我需要底下的什麼」，不是「我錨在哪一邊」。** 錨在哪一邊會隨視窗寬度變，
**由 CSS 決定，只看視窗寬度**，不問指標粗細
（[ADR-0023](docs/adr/0023-width-places-things-pointer-sizes-them.md)）。線在 820px。
JavaScript 拿同一組（`needs` ＋ 寬度）決定行為，兩邊各推導各的，誰也不等誰。

推導出來的結果：

| `needs` | 桌機（≥820px） | 手機（<820px） |
| --- | --- | --- |
| `"page"` | 右邊一欄，sunken，不變暗，不 trap，✕，往右滑 | 底部 sheet、書留在上面，raised，不 trap，✕，往下滑 |
| `"book"` | 右邊一欄，sunken，不變暗，不 trap，✕，往右滑 | 全螢幕，raised，trap，✕，往下滑 |
| `"nothing"` | 蓋在整個視窗上，raised，**變暗**，trap，✕，往右滑 | 全螢幕，raised，trap，✕，往下滑 |

**trap focus 與捲動鎖是同一個條件**：這張面有沒有蓋住整個畫面。

**面板一律進 hash，寫成 query 段**（`#/book/abc?d=toc/abc`）：底層路徑答「底下是誰」，`d` 答
「開著哪一張面」。這樣 Android 的返回鍵關的是面板而不是整個 app，重整之後面板還在。走路徑尾段
的話，每張面會有 N 條路由，N 是底層畫面數。⚠️ **`?d=` 放得下一個值，所以四張面互斥**：桌機上
開「關於」會關掉站著的目錄。**按上一頁退回結構上的上一層**，而〈筆記〉有兩層（列表、某一筆）。
細節見 ADR-0046。

**寬畫面推開書，窄畫面把底下兩層趕走。** 兩邊付的是不同的帳，換到的是同一件事：**面板底下要看得到
真的書**。

- **≥ 820px**：書讓出右邊一欄，frond 跟著重新分頁。這一筆是〈目錄〉與〈筆記〉在付，開關一次頁碼就
  動一次。原本不推的理由是「問我在哪不該把我移走」，那句話對〈目錄〉還成立，對〈排版〉不成立，
  而兩個面板兩種行為就是兩套 bug。
- **< 820px**：沒有欄可以讓，所以 entries 與 Scrubber 退場，書一格都不動。〈找〉那條「Scrubber 恆在」
  因此只適用寬畫面，三層疊起來的結果是三層都看不清楚。**〈目錄〉與〈筆記〉在這裡佔滿整個畫面**：
  剩下的空間本來就放不下一本讀得下去的書，留一條書縫換不到東西。**〈排版〉不能**，它就是那個
  要邊看書邊調的，所以它留在底部升起、書留在上面。

**有輸入框的地方貼在可用區域的上緣**，而不是跟著面板從底部升起
（[ADR-0044](docs/adr/0044-the-keyboard-takes-the-bottom-so-typing-goes-to-the-top.md)）。可用區域是
面板分到的那一塊：窄畫面上是整個畫面，寬畫面上是右邊那一欄。理由是虛擬鍵盤會拿走下緣，而版面拿不到
它有多高。

**三個面板不常駐**，三個狀態互斥那條沒有例外（[ADR-0020](docs/adr/0020-the-interface-steps-behind-the-book.md)）。
那支 ADR 拿掉的兩個側欄是常駐的，這一個是〈找〉的一部分：它跟著 chrome 一起走，點內文收掉面板、
再點一下收掉 chrome。（〈書的詳情〉不在〈chrome〉裡，它從書架也開得到。）

_Avoid_: sidebar／側欄（指得到舊那兩個常駐的，而那正是被拿掉的東西）、sheet（那是〈排版〉在手機上的
長相，不是它的名字）、抽屜（這個詞已經刪掉，見上）、對話框（dialog 是實作長相，面板講的是
「疊在誰上面」）

### Scrubber

底部的「拖拉定位軸」：讓使用者拖或點就快速跳到全書任一位置的控制項，和 Navigator（一次翻一頁）
互補。以 frond 的整書 fraction 為座標；拖拉中只更新預覽泡泡（章節標題＋百分比），放開才真的跳。
左開本視覺 LTR、右開本鏡射（見〈翻頁方向〉），並用兩端極簡圖示（書首實心／書末空心）＋ 填色起點標示方向。

**索引建好之前是 disabled**：fraction 在 frond 建完整書索引前是 undefined，畫成 0% 會是謊話。

_Avoid_: 進度條、progress bar、slider、seek bar（都指同一個東西，統一叫 Scrubber）。

### 唯讀進度條 / Readout

**不是 Scrubber**，而且畫出來就是為了不像它。目前只有一個：字型下載那條。

灰的（`--text-muted`，永遠不用潮色）、固定 3px、沒有 thumb、沒有端點、`pointer-events: none`、
`role="progressbar"`。它**不佔自己一列**，緊貼在它所解釋的那行字下面。

兩者的差別要一眼看得出來，因為它們會同時出現在〈找〉狀態裡：把 Scrubber 誤認成進度條不花任何
代價，反過來誤認一次，讀者就被丟到書的別處，而且沒有「回到剛才」。守門的測試在
`packages/app/tests/browser/reader/draggable.spec.ts`。

_Avoid_: 進度軸（那是 Scrubber）、loading bar（它報的是位元組，不是「畫面還沒好」）

## 書怎麼排 / How the book is set

### 行長 / Line length

一行放得下幾個字。**直排橫排是同一件事**：橫排的一行是欄寬，直排的一行是文字區的高度，只是落在
不同的軸上。單位是 **em**，因為對方塊字 em 就是字數，而且讀者調字級時行會變寬、字數不變。

有**上限**（方塊字 40 em、拉丁 30 em，見 [ADR-0012](docs/adr/0012-line-length-has-a-ceiling-measured-in-ems.md)），
**沒有下限**：行太短的代價是多回掃幾次，字太小的代價是看不清楚，所以不為了湊行長去縮字級。手機橫排
就是 18 字，那是接受，不是待修。

**留白是它的餘數**，不是獨立的一件事：可用寬度＝留白×2＋行長×欄數＋欄間距×(欄數−1)，定了兩個，第三個
就沒得選。所以面板上的「留白」是**下限**（至少留這麼多），寬螢幕上真正決定版心的是行長上限。

_Avoid_: 版心（那是「行長×欄數＋留白」整塊，不是一行）、行寬（橫排讀起來對，直排就錯了，直排的一行
是垂直的）、measure（英文排版的詞，這份文件用中文）

### 書寫系統 / Writing system

一本書的字**是不是一個 em 寬**：方塊字（中文、日文、韓文漢字）一格，拉丁一半。〈行長〉的上限靠它
分兩個數字。

**從內文判斷，不看 `dc:language`**，metadata 說謊是常態（有一本繁體中文書宣告 `ja-JP`，內文 0 個
假名）。開書時就著已經取好的那 5000 字樣本數一次，CJK 佔比**達一半**就是方塊字，剛好一半歸方塊字，
跟「一個字母都沒有」時的預設同一個方向。

_Avoid_: 語言（它答的不是「這本書是什麼語言」，而且會讓人拿去決定字型或〈介面語言〉，那是別的
判斷，而且現在真的有一個那樣的東西了）、簡繁（`detectVariant` 那個，答的是另一個問題）

### 自帶字型 / Carried font

Tidemarks 自己那一份 Noto CJK，**在這台裝置上有沒有**。讀者開了真的有漢字的書才下載，位元組存進 Dexie，
要用的時候做成 `blob:` URL 交給 frond（見
[ADR-0014](docs/adr/0014-spine-carries-the-font-rather-than-borrowing-it.md)）。

它換到的是**可預期性**，不是「有字可用」：後者由 `chinese.ts` 的字型堆疊負責，自帶字型解的是「同一本書
在每台機器上是同一個字型畫的」。所以本機已經裝了 Noto 也照下不誤，因為本機那份的版本不可控。

**一種字型一支檔案，而且是可變字型**（黑體 12.86 MB、明體 18.83 MB）。以前是 Regular 與 Bold 兩支
靜態檔；換過來省 43%，代價是沒有「先拿到 Regular 就能開始讀」那個中間狀態了（[ADR-0035](docs/adr/0035-one-variable-face-replaces-two-static-ones.md)）。

**觸發下載的判定跟〈書寫系統〉不是同一個**，兩者的安全預設方向相反：書寫系統數不到字時歸方塊字，因為
較窄的行長兩邊都不糟；自帶字型數不到字時不下載，因為誤下載是讓讀者在背後付 19 MB。

**介面跟它借**，但只借明體、也只在**下一次開 app** 的時候借（`lib/ui-font.ts`）。書在 iframe 裡，
`@font-face` 是 per-document 的，所以頂層文件要自己再註冊一次；借到之後介面的中文粗體才是真的 Bold
字形，不是瀏覽器合成的。介面借的是**整條字重軸**，不是讀者那兩個字重，那兩個是書的，而介面有自己的
級距。不跟著〈排版設定〉的 `fontFamily` 走：那個設定的語意是「**書**該長什麼樣」。
拉丁那半不必借，Tidemarks 自己帶了 Source Serif 4（90 KB，跟 app 一起載）。

⚠️ **英文名是 `Carried font`，但檔案叫 `web-font.ts`。** 這是現存的落差，不是兩個名字：`web font`
在 _Avoid_ 裡，理由見下。要嘛哪天把檔案改名，要嘛就記得它們是同一件事，別再多生一個叫法。

_Avoid_: 內嵌字型（暗示跟 app shell 一起打包，而它正好相反，不進 precache）、web font（太泛，書自己
也可能帶字型，那是書的資源不是 Tidemarks 的）

## 讀到哪 / Where the reader is

### 閱讀位置 / Reading position

讀者在一本書裡讀到哪：一個**點**（`progress` 的 `cfi`）。它跨裝置有意義，所以它同步、走 LWW，是 server 上關於「讀到哪」的唯一答案。

⚠️ LWW 有一個例外，見〈回訪模式〉：讀者回頭去看自己劃過的一段話的時候，這個位置只前進、不後退。那個例外只活在該台裝置的記憶體裡，server 不知道有這回事。

跟 frond 的**頁**分開講，因為兩者答的是不同的問題：閱讀位置是一個點，頁是一個範圍，而且頁是版面的產物，換個螢幕、換個字級就是另一組頁。所以 **server 永遠算不出讀者現在那一屏上是哪些字**，而讀者嘴上說的「我現在看到的」正是那一屏。

這條界線在 MCP 那邊承重（[docs/specs/mcp/spec.md](docs/specs/mcp/spec.md)）：agent 要回答「解釋我現在看到的這段」，就得由 client 在發問的那一刻把當前那一屏的範圍送上來，**那一刻過了就永遠算不回來**。所以 `progress` 除了 `cfi` 這個點，還存一個 `pageRange`（frond 的 `RenderLocation.pageRange`，一個 range CFI）：那是**當時那一屏**，不是位置的另一種寫法，Worker 靠它才有東西可讀。`pageRange` 是 `null` 的時候（整頁圖，或那一列是這個欄位存在以前寫的），MCP 只能退回讀位置附近的一段，而且會在答案裡講明那不是「你看到的那一頁」。

**網址講得出位置**：`#/book/<id>?at=`，三種寫法：`cfi:`（精確到字）、`chars:<section>[/<字數>]`（某一章，或那一章裡第幾個字）、`frac:0.4`（整本書的進度）。**沒有「第幾頁」**，理由跟上面同一條：頁是版面的產物，換個字級就是另一組頁，所以拿頁當座標的網址，明天指到的是另一段話。

⚠️ **這個網址只在開書的那一刻讀一次，翻頁不會寫回去。** 也就是說它說的是**你要求的位置**，不是**現在在哪**（後者是〈閱讀位置〉，住在資料庫裡）。會寫回網址的只有一種情況：從筆記面板跳到某一條重點的出處，那時候網址跟著換（`replaceState`，不留歷史紀錄），讓那一頁貼得出去。

**這一條、〈別處的位置〉與〈回訪模式〉三件事的狀態合在 `src/lib/place.ts`。** 三個概念沒有變成一個，
但它們在閱讀器裡是同一組狀態一起動的：接受別處的位置會結束回訪、回絕它會把畫面上那一頁寫成位置、
回訪期間的 `relocate` 只移動畫面。分開放的時候，每一條都是呼叫的人要記得的規矩；合在一起之後，
它們是一次有名字的轉換，而且順序測得到（`src/lib/place.test.ts`）。

_Avoid_: 進度（`progress` 是那張表的名字，但這個詞聽起來像百分比，而位置的主體是 CFI）、當前頁（那是 frond 的「頁」，是範圍不是點）

### 別處的位置 / Position from elsewhere

同一本書在別的地方寫下、而且比這台新的那一筆〈閱讀位置〉。

**只在閱讀器裡有意義。** 書架上沒有這個東西，從書架開一本書的那一刻本來就取兩邊比較新的那筆，
問題只發生在書**已經開著**的時候：閱讀器只在開書那一刻問過一次位置，之後同步拉下來的新位置寫進
資料庫，畫面不會知道，而下一次翻頁就把它蓋掉了（[ADR-0037](docs/adr/0037-a-position-from-elsewhere-is-offered-not-taken.md)）。

**「別處」是刻意含糊的。** 位置裡沒有裝置的身分，所以 Tidemarks 知道的就只是「有人在別的地方寫了
比較新的一筆」，是哪一台、甚至是不是同一台的另一個瀏覽器，它都答不出來。文案照這個事實寫。

⚠️ **畫面上那條 banner 只有這一個來源。**〈回訪模式〉曾經也會升起同一條，2026-08-28 拿掉了
（[ADR-0040](docs/adr/0040-a-visit-is-marked-on-the-rail-not-over-the-book.md)）。文案仍然不提「別處」，理由是上面那條：
Tidemarks 答不出是哪一台。

⚠️ **書還沒排完版的時候，這筆位置是收著的，不會升起 banner。** banner 蓋在整個閱讀器上，而它的兩顆
按鈕都要有 renderer 才動得了。書開到一半升起它，〈Go there〉會走到一個還不存在的 renderer，什麼都沒
發生，然後把提議清掉。收著的規則在 `src/lib/place.ts`（`ready`），〈閱讀位置〉那條說了為什麼那三件事
的狀態住在同一個地方。

_Avoid_: 另一台裝置（那是猜的，見上）、衝突（兩邊都是同一個讀者的真實行為，沒有誰錯）、更新的進度
（「更新」聽起來像該自動採用，而這整條規則就是不自動採用）

## 回訪 / Revisiting

### 回訪模式 / Visit mode

**讀者回頭去看自己劃過的一段話，這段期間閱讀進度只前進、不後退。** 讀者讀到第一百頁，按了一條第五十頁
的重點，進度仍然是一百頁；在五十頁前後翻幾頁也還是一百頁。「算不算回訪、算不算讀回來了」這兩個判斷
住在 `src/lib/visit.ts`，**什麼時候問這兩個問題**則是 `src/lib/place.ts`（見〈閱讀位置〉）。狀態只活在
閱讀器的記憶體裡，不存進 IndexedDB、不同步、重載就沒了。

⚠️ **英文名是 `Visit`，不是 `Revisit`**，雖然中文兩個詞都用「回訪」。分法跟程式碼一樣：這個模式住在
`visit.ts`（`VisitPosition`、`entersVisit`、`leavesVisit`），而〈回訪卡 / Revisit card〉住在
`revisit.ts`。兩個是不同的東西，中文剛好共用一個詞。

⚠️ **這個模式不打斷閱讀**（[ADR-0040](docs/adr/0040-a-visit-is-marked-on-the-rail-not-over-the-book.md)）：沒有橫幅，沒有蓋住
書的東西。畫面上只有一個地方講這件事：**定位軸上一顆小記號**，站在被保留的那筆進度的位置，寫著
`↩ 47%`。按下去回到那一頁，而回到那一頁就等於讀過了，回訪因此結束，記號跟著消失。索引還沒建好的
那段時間記號照樣畫出來，但跟定位軸一起是灰的、按不動：那時候整本書還沒分頁，沒有地方可以跳。

**進入**：從〈回訪卡〉開書，或在〈筆記〉面板點一條重點，**而且**那段重點不在讀者現在看的那一頁上。
點一條就在眼前的重點什麼都不會發生。

**離開**有兩條路：位置超過原本的進度，或回到原本進度那一頁。離開這本書或重載也結束，而那種結束不寫
任何東西，進度留在原處。按定位軸上那顆記號走的是第二條，它只是把讀者送回那一頁，結束回訪的仍然是
到達本身，不是那顆按鈕。

還有半條：〈別處的位置〉那條 banner 剛好站著的時候，它的兩顆按鈕都會結束回訪，〈Stay here〉還會把
進度改成現在這一頁。那是同步那一邊的事，回訪自己沒有這個入口。

⚠️ **這是〈閱讀位置〉「最後寫的贏」唯一的例外**，而例外只在這台裝置的記憶體裡，同步那一層完全不知道
有這回事。〈Stay here〉是全 app 唯一能讓進度往回走的動作：自動的行為只前進，**讀者明說的話可以往回**
（[ADR-0037](docs/adr/0037-a-position-from-elsewhere-is-offered-not-taken.md) 的原則用在同一條 banner
的另一半）。

**規則長在位置上，不長在動作上。** 「哪些動作算回訪」這種分法碰到定位軸就答不出來，讀者拖它的理由
有兩種，而畫面上分不出是哪一種。改成看位置就不必問意圖：拖到六十頁不算前進，拖到一百二十頁就算。
同理，**目錄與定位軸不是入口**：用目錄跳回第三章重讀是真的在讀，那種要照常更新進度。

⚠️ **判斷「是不是同一頁」一律問 frond 現在的畫面**（`renderer.location`），不要讀存下來的
`Progress.pageRange`。frond 的 `relocate` 去重時不看 `pageRange`，所以桌機打開〈筆記〉面板讓書讓出
一欄的那次重排會被吞掉，存起來的分頁就停在舊版面，拿它去比對，讀者點一條明明在眼前的重點，進度也會
被凍住。⚠️ 這種壞法在畫面上只剩一個影子：定位軸上多了一顆本來不該出現的記號。沒注意到的話，要到下
次開書發現進度停在原地才知道。
唯一的例外是從〈回訪卡〉開書：書還沒排版，沒有版面可以問。

_Avoid_: 造訪模式（〈回訪卡〉已經用「回訪」，一個概念一個詞）、預覽模式（沒有「預覽」這回事，讀者
真的在讀那一段）、鎖定進度（聽起來像讀者要自己開關的功能）

### 回訪卡 / Revisit card

**書架第一屏最上面那張卡，每天端出一條重點。** 重點本來只活在閱讀器的〈筆記〉面板裡，而那個面板一次
只講一本書，所以在某本書劃過的句子，除非重新打開那本書，否則不會再出現在眼前。這張卡是為了讓它出現。

它要守的事情比「讓重點再出現」再具體一點：**讓一條重點在讀者還說不出它為什麼重要的那段期間活著，
並且反覆回到眼前。** 那段期間可長可短，資訊書可能當天就能把想法寫成自己的話，文學可能要好幾個月，
但走的是同一條路：被打中 → 說不出為什麼 → 反覆遇到 → 慢慢說得出來。

⚠️ **搜尋幫不上這件事**，因為讀者不知道要搜什麼。一條還說不出為什麼喜歡的句子，連它跟什麼有關都
還不知道。搜尋、標籤、雙向連結全部預設「先知道自己在找什麼」，那是這個迴圈的後段；這張卡做的是前段。

**它是一個入口，不是回訪發生的地方**（[ADR-0038](docs/adr/0038-the-revisit-card-is-a-way-in.md)）。
卡上是一段引文、讀者自己寫的筆記（沒寫過就沒有這一段）、上面一列小字說這是什麼、來自哪本書、多久以前
畫的，還有一顆換一則。**按引文回到書裡那一段**；想寫東西，在那裡寫：閱讀器的〈筆記〉面板本來就能編輯，
多按一次，換掉的是書架第一屏上一整個寫字的區塊。留一個空輸入框在書架上，等於在那一屏放了一張
欠填的表。

回到書裡那一段**不會改動閱讀進度**，見〈回訪模式〉：讀者讀到哪裡跟他回頭看了哪一句是兩件事。

**當天固定。** 讀者可能還說不出為什麼喜歡，而每次回到書架都換一則就永遠不會有機會想清楚。換一則是
讀者自己按的，按了之後那一則就是今天這一則（重新整理不會把它換回去）。

⚠️ **沒有「關掉」這個動作，只有「換一則」。** 只要一個東西可以被清空，它就變成待辦事項：某天沒清
就欠著，然後讀者開始避開它所在的那一屏。換一則是讀者主動要，不按不會累積任何東西。

⚠️ **不是跨書的筆記清單。** 照時間把所有重點列成一條流讀起來像留言板；一次一條是刻意的。系統也不替
讀者把兩本書裡的句子連起來，Tidemarks 提供工具，整理是讀者自己的事。

_Avoid_: 輪詢卡（舊名，「輪詢」是 polling，跟這件事無關，而且會被讀成自動輪播）、複習卡（這裡沒有
考核也沒有記憶強度，那是 Anki 的語彙）、筆記牆／時間軸（那是被否掉的那個形狀）、carousel（英文詞，
而且它沒有自動輪播）

### 重點的抽法 / Daily draw

〈回訪卡〉今天那一條是怎麼來的：**從全部重點裡隨機抽一條**，抽中的寫進不同步的 `meta` 表，當天不再
改變。按「換一則」就再抽一次，唯一的保證是**不會抽到剛剛那一條**（整個書架只有一條重點的時候除外，
那時候給同一條也好過給一張空卡）。

⚠️ **這是刻意退回來的最簡單作法，而且它有一個已知的代價。** 純隨機不記得誰出現過，所以剛看過的那條
明天被抽中的機率跟兩年沒出現的那條一樣；收藏越大，被冷落的那批越大，而畫面上不會有任何東西說有一批
重點從來沒被抽到過。以前的作法是照「上次出現在卡片上是什麼時候」排序再抽，量過的數字與換回去的觸發
條件寫在 [ADR-0038](docs/adr/0038-the-revisit-card-is-a-way-in.md)。

**`lastShownAt` 還在寫，只是沒有人讀。** 它是那個排序唯一需要的欄位，而一個停止寫入的欄位，等到要用的
那天，中間那段時間是空的。它跟著重點同步（「我看過這句了」是**讀者**的事實）；「今天是哪一條」不
同步，那是這台螢幕的事實。

_Avoid_: 複習排程／SRS（沒有記憶強度、沒有評分、答錯不會更頻繁）、隊伍（沒有排隊這回事了，是抽的）

## 那面牆與它的順序 / The shelf and its order

### 書架 / Shelf

讀者的那一面牆，也是畫面上那個詞（按鈕與空狀態都寫「書架」）。它是一個結構：**書、進度、閱讀時段、
重點**四塊，由 `src/lib/shelf.ts` 的 `shelfProjection` 從 IndexedDB 的四張表算出來，畫面上的每一樣
東西都從它長出來。

那個投影守著一條看不見的規則：**刪掉的書把它的重點一起帶走**。刪除留的是墓碑不是真的刪掉列，所以
書沒了以後它的重點仍然是活的一列，同步把刪除帶走以前都還在，沒有這條，那些句子會一直留在第一屏
上，引用一本讀者已經沒有的書。

書架**怎麼排**不在投影裡，見〈最近碰過〉與〈書名排序〉：排序要看〈介面語言〉，而上面那條規則跟語言
無關。

_Avoid_: 書櫃、library（畫面上那個詞是「書架」）、書單（那是要讀的清單，不是手上有的書）

### 正在讀 / Reading now

書架第一屏那一橫排的主角：**有 `progress`、而且還沒讀完的書裡，`lastReadAt` 最晚的那一本**。只讀了 1%
也算，讀者確實停在那本書的中間。

**它認得出來靠的是動作，不是尺寸**：那一排帶著整屏唯一的主要動作（〈Keep reading〉）。以前它是一本
大書，靠體積跟牆上的封面拉開差距；換成〈回訪卡〉當主角之後那條路走不通了，而動作這條比尺寸耐用，
尺寸會被下一次改版稀釋。同一本書**在封面牆上也看得到**，排在第一格，那不算重複：一個是動作，一個是
書櫃裡的一本書。

看 `lastReadAt` 而不是〈最近碰過〉的 `max(lastReadAt, addedAt)`，因為兩者答的不是同一個問題：〈最近
碰過〉排的是整個書架，剛匯入的書該浮上來；「正在讀」問的是「我讀到一半的是哪一本」，而今天早上匯入
的書不是。

**一本都沒有的時候那一排就不出現**（全新的書架，或全部讀完），第一屏是〈回訪卡〉加封面牆，一條重點
都沒有的話那張卡也不出現，只剩封面牆。把一本沒
翻開的書擺成「接著讀這本」像在催人。「讀完」是 `percentage ≥ 0.99`，理由見
[spec](docs/specs/ux-replan/spec.md#哪一本是正在讀)。

_Avoid_: 當前書、current book（聽起來像「開著的那一本」，而這個詞在書架上才有意義，閱讀器裡沒有第二
本書要跟它比）

### 最近碰過 / Last touched

書架的預設順序：`max(lastReadAt, addedAt)`，也就是**上次讀到哪一本**與**剛匯入哪一本**共用的那個時刻。

用 max 而不是純 `lastReadAt`，是因為書架要同時答兩件事，而它們其實是同一件：讀者剛做過什麼。
**匯入也算一次碰過**，沒讀過的書根本沒有 `progress` 那一列，純 `lastReadAt` 會讓剛匯入的書排在所有
讀過的書後面，匯完還要自己找。

排序**不看進度百分比、不看累計閱讀時間**。那兩個答的是「這本書我讀了多少」，是回顧；書架答的是
「接下來開哪一本」。

〈書名排序〉是另一個選項，不是它的一部分。**兩個就是全部**，作者分組沒做（真書的 `dc:creator`
髒到 34 本分出 32 群），系列沒做（那 34 本一本都沒宣告 `calibre:series`）。

_Avoid_: 最近閱讀（少掉匯入那一半，而那一半正是 max 存在的理由）、last modified（那是檔案的事）、
書櫃（那一面牆叫**書架**，畫面上的按鈕與空狀態都這樣寫，詞彙表不該另起一個）

### 書名排序 / Title order

書架按書名排時用的 collation。**綁〈介面語言〉，不綁 `navigator.language`**，介面與書架永遠是
同一種語言。讀瀏覽器語系的話，把介面設成繁中的讀者會拿到中文介面配 Unicode 碼序的書架，兩邊都
不對。

排出來長什麼樣因此跟著讀者選的語言走：繁中是筆畫序，英文不是。這在 2026-08-21 以前是個
理論問題，那時介面語言是寫死的常數，只有一種可能的答案。

`numeric` 開著：關掉的話「第 10 集」排在「第 2 集」前面。

_Avoid_: 筆畫排序（筆畫只是繁中的答案，換個介面語言就換一種）、字母序（中文沒有）

## 讀者留下的東西 / What the reader leaves

### 接管選取 / Owned selection

觸控上，Tidemarks 自己做的 text selection，取代瀏覽器原生那一套
（[ADR-0036](docs/adr/0036-touch-selection-is-ours-not-the-browsers.md)，實作見 issue #49）。長按
吸一個詞、拖端點延伸、反白與顏色列都是 app 自己畫；frond 只補一個事實（`rangeFromPoints`：兩個容器
座標點 → 一段範圍，issue #50）。

**只在觸控上接管，桌機留原生。** 桌機沒有那些症狀（滑鼠不翻頁、不會自動捲進下一頁、沒有搜尋 bar），
而原生選取免費帶著可及性（[ADR-0021](docs/adr/0021-accessibility-is-borrowed-not-written.md)）與鍵盤
選字。依 `pointerType` 分流：手指走這一套、滑鼠留給瀏覽器。

**這一輪只做單屏內選取**，不跨頁，這樣「拖到邊緣自動捲動延伸」那整套從源頭不存在，而那正是原生選取
跟 frond 分頁打架的地方。

**選取中的反白是一塊淡潮色的底，不是波浪線。** 波浪線是〈重點〉畫出來的樣子，而重點是**墨**，劃完
留下來的東西；還在選的那一段是**潮**，它可以動。兩者用同一個形狀，讀者就分不出「我已經劃過了」跟
「我正要劃」，而蓼藍與潮色又正好是最接近的一組（見〈紙／墨／潮〉）。桌機的原生 `::selection` 用同一個顏色，
所以兩套選取在畫面上是同一件事。

**兩個端點手把是〈潮〉，摸得到的比看得到的大。** 圓珠 11px、hit 區 44px，直排時整組轉九十度掛到字的
左右兩側。看得到與摸得到是兩組框，跟〈重點〉的畫框／點框同一種分法（數字在 `docs/design-system.md`）。

它跟〈標〉是兩件事：接管選取是「**怎麼選到那段字**」，〈標〉是「選到之後畫面長什麼樣」。選取一出現就
進〈標〉，但**手指還按著的時候不算**，顏色列要等放手（見〈chrome〉）。它也小幅修訂 Navigator「時間
不參與判斷」那條：時間開始拿來認出長按選字，但永遠不拿來擋一次翻頁（理由見 ADR-0036）。

**有選取的時候，按在手把以外的地方照樣翻頁**，翻頁順手把選取清掉。原本「按下那一刻有選取就不翻頁」
是原生選取年代的補償，那時候 app 分不出手指落在手把上還是別處，只能一刀切；手把改成自己畫之後，那個
限制不存在了。

**網址也可以指定選哪一段**：`?select=` 收一個 CFI 或一段文字，開書那一刻讀一次，讀完就跟真人選的一模
一樣：顏色列浮出來，點一下就劃出一條重點（`lib/route.ts`、[ADR-0043](docs/adr/0043-automation-walks-the-reader-s-path.md)）。

**它預設強制走原生那一套，不管視窗多大**，要接管的那一套才加 `?handles=1`。兩個預設的失敗方式不一樣：
預設原生的話，手機尺寸下截出來的圖少了那兩顆把手，這件事看得見；預設接管的話，桌機尺寸下什麼都不會
發生，而畫面上沒有任何東西說明原因。**選看得見的那個。**

⚠️ 強制原生只撐到下一次 pointer 事件為止（`ownSelectionRef` 每次都會翻轉），所以套上之後再 tap 一下，
手機尺寸下的選取就沒了。這個參數是打字打出來的，用它的人不會同時在讀那本書，所以不另外擋。

_Avoid_: 自製選取／custom selection（都漏掉「只接管觸控、桌機留原生」這半，而那半是整個決定的重點）

### Highlight layer

把〈重點〉畫到書上面的那一層。frond 不畫 highlight（它給 `rectsFor(cfi)` 的真實幾何與 `layout`
事件），所以顏色、透明度、深色模式的混色、以及「點一下開筆記」都是這一層的決策（用哪四個顏色見
〈重點〉）。

⚠️ **`mark` 跟 `highlight` 在程式碼裡是兩個層次，不是同義詞**：`mark` 是**讀者留下的那個東西**
（`Annotation` 這筆資料、`--mark-indigo` 這組色、`markVar()`），`highlight` 是**把它畫上去的那
一層與那件事**（`HighlightLayer`、`.highlight-box`、`lib/highlights.ts`）。兩邊各守一個層次，
所以程式碼裡的 `highlight*` 全部留著；中文文件一律寫「重點」。

- 幾何與顏色政策是純函式（`lib/highlights.ts`）：`visibleBoxes` 把不在當前頁的矩形裁掉，裁的是
  frond 的 `pageBox()` 而**不是容器**，容器含著留白，下一頁的開頭會落在裡面（#41），
  `boxesContain` 做 hit test，`markVar` 決定用哪一種〈重點〉的墨。
- 元件（`HighlightLayer.tsx`）只負責畫，**而且 `pointer-events: none`**，點 highlight 走 frond 的
  `pointerup` 加自己的 hit test，否則這一層會吃掉翻頁的點擊。

_Avoid_: annotation layer（`Annotation` 是資料，這是畫出來的那一層）, overlay（太籠統）

### 重點 / Mark

**讀者在書上選一段話留下來的那個東西。** 產生它的動作叫「劃重點」（「劃線」是口語的同義說法，
介面上不出現）。一則〈筆記〉可以掛在它上面，兩者存成同一列 `Annotation`。

它有四個**墨水色**：蓼藍 `indigo`、赭石 `ochre`、苔綠 `moss`、松煙 `soot`。
**預設的蓼藍就是品牌記號色**，不改設定的話，讀者劃的重點就是這個 app 名字的意思。

四色都跟〈潮〉分得開，所以「我劃的」跟「我可以按的」不會混。蓼藍與潮色是其中最接近的一組，
**靠形狀分不靠顏色分**：一個是字邊 4px 的波浪線，一個是實心的控制項。

**`Annotation.color` 存的是色名，不是色值**，這是為什麼換一整套顏色不需要 migration，也是為什麼
`lib/highlights.ts` 的 `markVar()` 回傳 `var(--mark-ochre)` 而不是一個 hex：亮暗兩套值住在
`styles/tokens.css`，讀者換主題的時候不必重畫。舊的四個名字（`yellow` / `blue` / `green` / `pink`）在那裡
對到最近的一個新色。

**它畫出來是這個樣子**，亮暗一致：**只有一條 4px 的波浪線，沒有底色**，潮水退去留下的痕跡，
全 app 唯一的裝飾，而且只出現在屬於讀者的東西上。橫排走行的下面、直排走字的右邊（傍線的位置）。
⚠️ 那條線**沒有自己的名字**，它是重點畫出來的樣子，不是另一個東西。

落點一句話（[ADR-0032](docs/adr/0032-a-mark-goes-beside-the-text-and-nothing-else.md)）：
**一行的記號畫在這一行所有墨跡的最外緣之外，貼著它，而且是一條不斷的線。** 「所有墨跡」含 ruby
的注音與上下標，所以它們只會把線往外推，不會把線切斷。畫與**點**是兩組框：注音與段落開頭的全形
空格不落線，但點得到。數字與量法在 `docs/design-system.md`〈重點〉。

英文名 `Mark` 來自程式碼（`--mark-indigo` 這一組 token 與 `markVar()`），⚠️ 跟〈chrome〉底下那個
`Marking` 差一個 ing：這裡是**讀者留下的那個東西**，那裡是**劃的時候畫面長什麼樣**。

_Avoid_: highlighter 那一類的說法（那種筆的定義就是一塊底色，而這裡只有一條波浪線，名字會跟東西
正好相反，這是廢掉舊譯名的理由）、動詞寫成「畫」（一律用「劃」）、標註（`Annotation` 是資料，含
筆記；重點是它的其中一半）

### 筆記 / Note

**讀者自己寫下的那一則，依附在一段劃下來的〈重點〉上。** 它是這個 app 的重心，閱讀體驗是基礎，
筆記體驗才是要做好的那一半（[ADR-0019](docs/adr/0019-the-app-is-called-tidemarks.md)），它扛的
是「反覆咀嚼」這個動作。

**一則筆記離不開一本書、也離不開一段選取**，這是範圍不是落差：Tidemarks 是一個劃重點、寫筆記的
工具，不打算變成通用的筆記系統（[ADR-0047](docs/adr/0047-a-note-hangs-on-a-mark.md)）。要把這些
字帶進自己的資訊系統是〈退路 / Exit〉那條在管的事。實作上它是 `Annotation.note`，跟重點同一列。

_Avoid_: 標註／annotation（那是**資料結構**的名字，一則筆記是它的內容之一）、highlight（那是
把重點畫到書上的那一層，本身不含文字）、note（`Annotation.note` 那個欄位名保留給欄位，敘述裡寫
「筆記」）

### 來源 / Source

**筆記是從哪裡來的那個東西。** EPUB 是第一種，不是唯一一種，文章與影片都要放得進來
（[ADR-0019](docs/adr/0019-the-app-is-called-tidemarks.md)）。

⚠️ **這個詞現在完全沒有對應的程式碼。** 今天資料模型裡只有〈書〉：`Annotation`、`progress`、
`ReadingSession` 全部綁 `bookId`，〈寄物櫃〉（R2）存的是 epub 檔。「來源」是為了讓往後討論
多來源的時候有一個共用的詞，**不是說現在有這個抽象層**。

在有第二種來源之前，講一本 epub 就講**書**，不要提前用「來源」去指它，那會讓現在的程式碼讀
起來像已經一般化了，而它沒有。

`bookId` 是**隨機 UUID**（`lib/epub.ts`），不是內容雜湊。所以同一本 epub 重新匯入會拿到新的
id，舊的筆記與進度對不回去，**那是對的**，重新匯入本來就是一本新書。

_Avoid_: 內容／content（太籠統，什麼都是內容）、item／entry（沒有講出「它是被讀的東西」）、
書（**書是來源的一種**，兩個詞不能互換）

## 介面說哪種話 / What the interface speaks

### 介面語言 / Interface language

讀者選的、**Tidemarks 自己那些字**用哪一種語言。三種：英文、繁體中文、日文。

**跟〈書寫系統〉是兩件事**，而且那一節的 _Avoid_ 早就把這條線畫好了：書寫系統答的是「這本書的字
是不是一個 em 寬」，它決定行長、字型堆疊與字形變體；介面語言只決定 Tidemarks 開口說話時用哪種
語言。一個日本讀者用日文介面讀繁體中文書是正常的一天。

跟著裝置，不跟著帳號（[ADR-0005](docs/adr/0005-the-reader-adjusts-their-own-reading-not-this-book.md)）。
自動從瀏覽器判斷，讀者可以在〈設定〉裡覆寫。伺服器寄的信也照這個語言走，靠請求上的
`Accept-Language` 帶過去。

它也決定 `<html lang>`，而那不是裝飾：自帶的 CJK 字型是 pan-CJK build，中日共用的漢字要靠
`lang` 挑出哪一種區域字形（[ADR-0031](docs/adr/0031-english-is-the-source-and-chinese-becomes-a-translation.md)）。

_Avoid_: 語系、locale（太泛，聽起來還管日期與數字格式；那些是跟著介面語言走的結果，不是它本身）、
語言（會跟書的語言混掉，那是〈書寫系統〉與 `detectVariant` 的地盤）

### 詞條 / Message

catalog 裡的一筆。身分由**英文原文**決定，加上有寫的話的 `context`，所以改一個英文字就是換一條
詞條，舊的變孤兒、新的沒有譯文。「翻譯過期」因此不是一種獨立的狀態，它就是「缺漏」。

一條詞條帶兩種給人看的東西，分工不同：**`context` 會進身分**（拿來把兩個一樣的英文拆成兩條），
**`comment` 不會**（寫給翻的人看的敘述，必填）。

Worker 的詞條是例外，它們自己取 id（`email.magicCode.subject`），理由見
`docs/agents/i18n.md`。

_Avoid_: 字串（`.po` 以外的字串多得是）、key（英文原文就是 key，講 key 會讓人以為要另外取名字）

### catalog

一種語言的全部詞條，一個檔案（`packages/app/src/locales/<locale>.po`）。原文那一份
（`en.po`）也是一個 catalog，只是它的譯文永遠等於自己。

_Avoid_: 語言檔、翻譯檔（沒有比較清楚，而且 catalog 是工具自己的用字）

## 讀者調得動的東西 / What the reader can adjust

### 排版設定 / Typography settings

**六項，一層，一份**（主題、字型、欄數、字級、行距、留白），存在這台裝置上。每一本書都照它渲染，
**調了就是每一本都調**（[ADR-0005](docs/adr/0005-the-reader-adjusts-their-own-reading-not-this-book.md)）。

一份設定，兩個入口，**同一個表單元件**：閱讀時是〈排版〉面板（疊在書上，上面留著真的書當預覽），
在書架上是〈設定〉這一層樓的〈排版〉tab。兩邊各寫一份的話，它們會慢慢長出不同的選項。

**沒有單本書的覆寫，所以也沒有「以後每本書都這樣排」那顆按鈕。** 剩下的唯一一顆是「回到預設值」。
別家的做法與這個決定的來由整理在
[docs/specs/reading-typography/prior-art.md](docs/specs/reading-typography/prior-art.md)。

**直排與橫排共用同一組字級與行距。** 這是「現在不做」而不是「決定不做」，回頭的證據寫在 ADR-0005。

**欄數**有三檔：自動、單欄、雙欄。自動照〈行長〉的規則決定（切成兩欄會讓每欄不到 28 字就不切）；明確
選了單欄或雙欄就照做，連手機上也不擋，**做不到才 disable，不好看照做**（直排 disable 是因為 frond 的
直排不支援多欄，那是做不到）。

**留白**是〈行長〉的下限，不是最終值：寬螢幕上真正決定版心的是行長上限，那時候調留白不會有反應。

**不跨裝置同步**（見 [ADR-0005](docs/adr/0005-the-reader-adjusts-their-own-reading-not-this-book.md)）：字級回答的是
「這個螢幕多大、我離它多遠」，那是裝置的事。這是刻意的，不是還沒做，bookId 兩台機器對得起來，缺的不是機制。

字級是**讀者瀏覽器 root 字級的百分比**，不是絕對 px（見
[ADR-0006](docs/adr/0006-font-size-is-a-percentage-of-the-readers-root.md)），同一個「大一點」，讀者在
瀏覽器裡講過一次就算數，Tidemarks 的介面本來就跟著，書也跟著。

_Avoid_: 全域設定（含糊，講不出「全域」的邊界其實停在這台機器上）、使用者偏好（暗示跟著人走，
正好相反）、裝置預設與書的覆寫（設定只有一層，這兩個詞的前提不存在）

### 設定 / Settings

一層樓（`#/settings/...`），不是面板，**三個 tab**，由近到遠：

- **排版**：那六項，加〈字型〉底下一行講這台裝置手上有哪支字（自帶字型的下載狀態，見
  [ADR-0014](docs/adr/0014-spine-carries-the-font-rather-than-borrowing-it.md)）。
- **帳號**：跨裝置的那一份，也是唯一會用到別人機器的那一半。
- **語言**：〈介面語言〉，一個控制項。排在最後是因為它最少被碰：排版是幾乎每天，帳號一年幾次，
  語言大概一輩子一次。它跟排版分開，是因為排版整組都在講**書**怎麼排，而語言一個字都不動書。

版本字串是**頁尾**，不屬於任何一個 tab：它回答「我在跑哪一版」，那是整個 app 的事。

閱讀畫面上沒有通往這裡的門，bar 上只有目錄、筆記、排版三顆。主題就在〈排版〉面板第一列，所以
「天黑了要隨手切深色」已經不必離開書；〈帳號〉是一年碰幾次的地方，走「‹ 書架」再進來就好。

_Avoid_: 偏好設定（跟〈排版設定〉的 Avoid 同一個理由）、選項、設定面板

## 帳號與伺服器 / Account and server

### 帳號 / Account

（決定與代價見 [ADR-0015](docs/adr/0015-an-account-is-only-as-strong-as-its-inbox.md)。）

**一個 email 一個帳號，email 是唯一識別。** 兩把鑰匙，都是正門，沒有主從：

- **passkey**：快，一鍵，走 discoverable credential，連 email 都不必輸入。
- **magic code**：六位數字寄到信箱，10 分鐘有效，五次試錯就作廢。它是**找得回來**的那一半。

**帳號的強度等於信箱的強度。** 這是刻意付出去的價：原本 passkey-only 換到的抗釣魚保護退掉一半，
換到的是「不會有人的筆記因為弄丟一張紙就沒了」。代價說出口，並且補一件很省事的事：magic code
登入成功之後寄一封通知信，被入侵的人幾分鐘內就知道。

畫面上 magic code 叫**登入碼**（讀者不必懂 magic code 這個詞）；程式碼與文件裡叫 magic code。

_Avoid_: 驗證碼（那是 CAPTCHA 的詞）、recovery code（**要拿掉的**，它扛的三件事都由 magic code
承接：passkey 全丟時的救援、換 hostname 時的搬家、收尾驗證的登入繞道）、magic link（寄的是碼不是
連結：連結會被 email client 先點掉，碼還可以在手機上讀、在電腦上打）

### 訂閱 / Subscription

一個帳號**現在有沒有伺服器那一半**：同步與 MCP 開著沒有。〈上線〉之後付款成功才建得了帳號，所以
帳號誕生的那一刻它一定是開著的；之後它會自己過期，而**帳號不會**。

**「有帳號、訂閱停掉、資料還在」是合法狀態，不是壞掉的狀態。**
[ADR-0011](docs/adr/0011-the-paywall-follows-the-monthly-bill.md) 承諾停止付費之後保留 24 個月，
那條承諾要求這個狀態活得下去。所以守門有兩道、位置不同：建帳號那一次看付款，之後每一次請求看訂閱
（見 [ADR-0016](docs/adr/0016-the-paywall-is-two-gates.md)）。

真相在金流商那邊，D1 存一份鏡像，webhook 負責更新。鏡像會落後幾秒，那是接受的，最壞的情況是剛付
完款的人多按一次同步。前端從 `GET /api/me` 讀它，**不從同步的 payload 讀**：那條路上的東西都走
LWW 合併，而訂閱狀態是伺服器單方面的事實，不該有任何一條路徑讓 client 寫得回去。

白名單那批人不在這個機制裡。他們是明知資料會被清掉才進來的，〈上線〉那天起維持免費，不設期限。

_Avoid_: 會員（暗示等級制，而 ADR-0011 只有一個級距）、付費使用者（那是人，這是帳號上的一個狀態，
而且訂閱停掉之後那個人還在）、`subscribeSync` 那個 subscribe（那是 sync 狀態的 observer，跟付費
無關）

### 開放註冊 / Open signup

註冊的閘門開著沒有。**沒開放的時候只有白名單上的 email 建得了帳號**，開放之後任何人都可以。

它擋的是**建帳號那一刻，不擋登入**，把一個人從名單上移掉，他的帳號還在、還登得進去。閘門控制
的是誰的資料進到伺服器上，而帳號一旦存在，資料就已經在了。

開關住在 `packages/app/wrangler.jsonc` 的 `vars`（`OPEN_SIGNUP`，沒設就是白名單），白名單住在 D1。分開是因為
**開放註冊是一個 commit，加一個朋友不是**：前者就是〈上線〉那條線本身，該在版控裡留下看得見的
痕跡；後者是日常操作，不該需要重新部署。開放之後**完全不查白名單那張表**，不是「表清空了所以
都通過」，切換的語意是換一條程式路徑，不是靠資料狀態。

錯誤訊息兩邊相反：白名單期間**講白**（「這個信箱還不能註冊」，那時候輸入 email 的是維護者認識
的人，含糊只會讓他對著空信箱枯等）；開放之後**一律含糊**（有沒有帳號都回同一句，否則任何人都問
得出某個 email 有沒有在用 Tidemarks）。

_Avoid_: 邀請制（沒有邀請碼這種東西，名單是維護者單方面加的）、註冊模式（「模式」在這份文件裡
是書寫模式那一類）、beta（講不出這裡真正要講的事：資料還可以清）

### 上線 / Launch

**開發階段**：現在。使用者是維護者自己，加上白名單裡那幾個人，所以**資料可以丟**，schema
要改就改，migration 裡直接砍掉重建也算數。**但丟之前要寄信通知、並且給得出匯出**：白名單裡
的人不是維護者，他們把整櫃書搬進來之前有權知道這件事會發生。這個承諾做得到，是因為到那時候
Tidemarks 手上第一次有 email。這段期間**付款走 Stripe test mode**，要驗的是流程跑不跑得通，不是
有沒有人願意掏錢，而收了真的錢就不能再說「資料可以丟」。

**上線**：**開放註冊**那一刻。白名單期間存在的理由就是那之前要做完的事（email 註冊、付款），
做完才開。從那天起 **migration 要把使用者的資料接過去**，不能再砍掉重建；CFI 的輸出穩定性也
變成契約。這條線刻意選成可觀察的事件而不是感覺，這樣 agent 自己判斷得出來現在站在哪一邊，
`OPEN_SIGNUP` 是設定檔裡讀得到的一行，比「`users` 表有幾列」更好判斷，也不會因為維護者自己多
開一個測試帳號就誤判（見 [ADR-0004](docs/adr/0004-development-phase-and-launch-line.md)）。

**這條線管的是 migration 的內容，不是要不要寫。** 動到 D1 的 schema 一律要在 `packages/app/migrations/`
加一支，那是它上到伺服器的唯一路徑，開發階段也一樣。翻面的是那支裡面能不能 `DROP`。

寫「註冊」不寫「付費」，因為**免費試用的帳號一樣有資料在伺服器上**。

_Avoid_: 部署、deploy、release、production（**這四件事都已經成立了**，`app.tidemarks.io`
是真的。它們講不出這條界線要講的那件事：資料屬於誰）、「開發階段不寫 migration」（曾經是這樣
講的，現在會讓人以為 schema 可以手動送上去。要說的是**開發階段的 migration 可以丟資料**）、
開放 email 註冊（**入口不是線**，email + magic code 在白名單期間就已經是註冊的唯一入口了，
翻面的是閘門的模式，不是入口的種類）、第一個非維護者的使用者（曾經是這條線的候選，但白名單
那批人是**明知資料會被清掉**才進來的，所以他們不翻這條線）

## 承諾 / Promises

### 退路 / Exit

開源要換到的東西，兩件具體的事：**維護者收掉服務之後，讀者自己架得起來**；**維護者漲價之後，
讀者搬得走**（見 [ADR-0009](docs/adr/0009-open-source-buys-an-exit-not-contributions.md)）。

它是好幾個決定的共同理由，所以值得有個名字：`worker/` 必須一起開源（只開前端就等於沒開）、
`docs/deployment.md` 必須讓一個外人照著架得起來（而且不需要自訂網域）、停止付費之後資料不刪、
**匯出的格式別人也讀得懂**（見 [ADR-0013](docs/adr/0013-your-copy-is-yours.md)，現在吐的是
一包只有 Tidemarks 吃得下的 JSON，那讓「搬得走」只搬得到另一個 Tidemarks，是 lock-in 不是退路）。

**退路不包含「伺服器看不到我的書」**，那件事開源換不到，MCP 又讓它永久出局，所以不承諾。
能承諾的只有免費的那一級，而且要用查得證的句子講：**不註冊的話，書、筆記、閱讀位置一個位元組都
不離開這台裝置**。舊寫法是「不註冊就完全不上傳」，那句字面上不成立，app shell 本身就是從 Worker
抓下來的，而一句經不起追問的承諾，被戳破一次比從來沒講過還糟。

**守著它的是 `packages/app/src/lib/session.ts`**：同步靠一個本機的旗標決定要不要發請求，沒登入
就一個都不發，不能靠讀 cookie 判斷（session 是 HttpOnly），也不能先打了再看 401，那時候東西已經
送出去了。查得證的那一半是
`packages/app/tests/browser/library/signed-out.spec.ts`：不註冊、匯入一本書、翻頁、切走再切回來，
斷言對 Worker 的請求數是零。

它同時是唯一擋著「在免費路徑上放個分析看看轉換率」的東西：**沒註冊的那段路上不放任何分析與錯誤
回報**。

_Avoid_: 開源（那是手段，不是要換到的東西）、可攜性（聽起來像匯出格式，而這裡講的是整套服務
搬得走）

### 寄物櫃 / Locker

Tidemarks 對使用者上傳的書所站的位置：**收下、保管、只還給本人**。這個詞由
[ADR-0010](docs/adr/0010-spine-is-never-the-path-to-a-book.md) 引入，
[ADR-0013](docs/adr/0013-your-copy-is-yours.md) 是它在檔案那一層的展開。

它是**形狀**，不是一句宣示，是一整組查證得了的事實：沒有任何分享功能、R2 一個使用者一份不
去重、沒有簽名 URL 也沒有公開網址、匯出原封不動、不掃描內容。**每一項壞掉都只要一個「加一下
也還好」的決定**，所以每一項都得有名字。

_Avoid_: 網路硬碟（那一類服務有分享連結，正好是 Tidemarks 沒有的那一半）、雲端書櫃（「書櫃」暗示
給人看，而寄物櫃的重點是只還給本人）、平台（Tidemarks 不是雙邊的，沒有第三人）

## 工具 / Tooling

### 畫面巡檢 / Screen sweep

一次走完 app **每一個畫面**、把每一張都截下來的那趟。腳本在 `packages/app/tests/sweep/`，
`npm run shots` 跑它，圖落在 `.scratch/shots/{desktop,mobile}/`，每次覆蓋。

**不管改了什麼都截圖**。這是它跟證據圖唯一但決定性的差別。它的圖是給人（以及跟人討論視覺的
assistant）看，看完就丟；它**不比對任何東西**，沒有基準圖，綠燈的意思只是「每一步都還走得通」。
在容器裡跑、只跑一家瀏覽器，理由與代價見
[ADR-0027](docs/adr/0027-the-screen-sweep-runs-in-the-container.md)。

**清單就是腳本本身**，沒有另一份索引檔，兩份必然漂移，而漂移之後沒有人知道哪一份是全站。

_Avoid_: 證據圖（**為了某一次改動**而截下來的那批，在 host 上產、預設 chromium 一家、要附數字與五項判讀，見
[ADR-0007](docs/adr/0007-pr-evidence-is-captured-on-the-host.md) 與
[pull-requests.md](docs/agents/pull-requests.md)）、視覺迴歸測試（巡檢**不做**比對，把它當成
迴歸測試會以為畫面壞掉時會有紅燈）、截圖（太寬，兩種圖都叫這個名字正是這一格要拆開的東西）
